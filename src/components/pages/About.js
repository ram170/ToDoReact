import React from 'react'

export default function About() {
    return (
        <React.Fragment>
            <h1>About</h1>
            <br></br>
            <p>This is a TodoList web-app using React js. I have already done one using pure js. I'm currently starting out with React and it's fun. React ftw</p>
        </React.Fragment>
    )
}
